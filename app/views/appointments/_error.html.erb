 
 <% if @appointment.errors.any? %>
	    <div id="error_explanation" style="padding-left: 700px;" >
	      <h2>
	        <%= pluralize(@appointment.errors.count, "error") %> prohibited
	        this article from being saved:
	      </h2>
	  
	        <% @appointment.errors.full_messages.each do |msg| %>
	        	<%= msg %><br>
	        <% end %>
	     
	    </div>
 <% end %>