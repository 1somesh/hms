
<% if current_user.doctor? %>
<h1 id="heading">Appointment List</h1>

  	<div class="list">
    <% @list.each do |appointments| %>
  	<br />
	        <div class="container" align="center" id="div<%=appointments.id%>" >
			        <div class="first_name" align="left"><%= appointments.patient.first_name %></div>
			        <div style="padding-left:50px ">
			            <%= appointments.date %><br />
 					</div>			         	
 			        <%= link_to 'Show', [appointments]%>
 			        <%= link_to 'Delete', [appointments],:method => "delete" %><br>
 			        <div align="left">Status: <%= appointments.status %></div>
			       </tr>
	    	</div><br />
    <% end %>  
    </div> 

<%= link_to 'Recent Appointments', "appointments/recent" %>
<% else %>

<br />
<div align="right"><%= link_to 'Create a new Appointment ', new_appointment_path(current_user.doctor_appointments.build) %></div>

 	<div class="list">
    <% @list.each do |appointments| %>
    	<br />
	        <div class="container" align="center">
	          <div class="first_name" align="left"> <%= User.find(appointments.doctor_id).first_name %></div>
		        <td><%= appointments.date %></td>
		        <br />
		      	<%= link_to 'Show', appointments%>
		        <td><%= link_to 'Edit', edit_appointment_path(appointments) %></td>
		        <%= link_to 'Delete', [appointments],:method => "delete" %><br>
		        <div align="left">Status: <%= appointments.status %></div>
		     </tr>
		 </div>
   <% end %>

<% end %><br>
</div>
